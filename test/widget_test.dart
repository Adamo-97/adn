import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder trimmed test so `flutter test` runs only our notifier unit tests.
  test('placeholder', () {
    expect(1, equals(1));
  });
}
